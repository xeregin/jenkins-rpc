---
- name: Provision dev cluster
  hosts: "{{hosts}}"
  gather_facts: True
  roles:
    - dev-lvm
    - dev-packages
    - dev-networking

- name: Provision dev cluster
  hosts: "{{hosts}}[0]"
  gather_facts: True
  roles:
    - role: dev-setup-git
      rpc_repo_dir: rpc
    - dev-setup-rpc
